{% extends "_layout.html" %}
{% set metatitle = entry.title %}
{% set metadescription = entry.description %}
{% set canonicalUrl = entry.url %}

{% block postHead %}
    <link rel="index" href="{{ site_url }}/recipes">
{% endblock %}

{% block content %}
    <article class="recipe">
        <header>
            <h1>{{ entry.title }}</h1>
            <small>Updated in <time datetime="{{ entry.date_updated.strftime('%F') }}">{{ entry.date_updated.strftime('%B %Y') }}</time></small>
        </header>
        {% if entry.featured_image|default %}
            {% image entry.featured_image %}
        {% endif %}
        {{ entry.body|render|safe }}
    </article>
{% endblock %}